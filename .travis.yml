language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1

sudo: false

cache:
  directories:
    - vendor
    - $HOME/.composer/cache

before_install:
  - mkdir -p build/logs
  - sudo apt-get -qq update
  - 'if [ $(phpenv version-name) == "5.5" ]; then sudo apt-get install -y php5.6-mongo; fi'
  - 'if [ $(phpenv version-name) == "5.6" ]; then sudo apt-get install -y php5.6-mongo; fi'
  - 'if [ $(phpenv version-name) == "7.0" ]; then sudo apt-get install -y php7.0-mongo; fi'
  - 'if [ $(phpenv version-name) == "7.1" ]; then sudo apt-get install -y php7.1-mongo; fi'

install:
  - composer install --prefer-dist --no-interaction

script:
  - phpunit --coverage-clover build/logs/clover.xml
  - vendor/bin/php-cs-fixer fix -v --diff --dry-run

after_script:
  - wget https://scrutinizer-ci.com/ocular.phar && php ocular.phar code-coverage:upload --format=php-clover build/logs/clover.xml
